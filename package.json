{
  "name": "utm-builder-neil",
  "version": "0.0.0",
  "private": true,
  "description": "Test harness for the privacy-first UTM builder. The tool itself is a single dependency-free HTML file; there is nothing to install.",
  "scripts": {
    "test": "node --test tests/"
  },
  "license": "MIT"
}
